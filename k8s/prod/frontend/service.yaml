apiVersion: v1
kind: Service
metadata:
  name: ai-agentic-app-frontend
  namespace: AI-Agentic-App
  labels:
    app.kubernetes.io/name: ai-agentic-app-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-agentic-app-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
