apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-agentic-app-backend
  namespace: ai-agentic-app
  labels:
    app.kubernetes.io/name: ai-agentic-app-backend
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ai-agentic-app-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
